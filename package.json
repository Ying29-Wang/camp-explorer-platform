{
  "name": "camp-explorer",
  "version": "1.0.0",
  "description": "Camp Explorer Platform",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "multer": "^1.4.5-lts.2"
  }
}
